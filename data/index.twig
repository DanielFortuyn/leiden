{# index.twig #}
{% extends "layout.twig" %}

{% block page %}
    <header>
        <h1>{{data.title}}</h1>
    </header>
    <p>
        This page is generated by Twig.js using the gulp-twig gulp plugin.
        {{dump(entries.items)}}
    </p>
      <ul>
        {% for item in entries.items %}
            <li>A {{ item.fields.title }}</li>
        {% endfor %}
    </ul>
{% endblock %}
<h1>jedsif</h1>asd
